---
# vars file for bind (Debian specific)

bind__packages:
  - bind9
  - bind9utils

bind__service_name: bind9

bind__default_file: /etc/default/bind9

# Directory with run-time stuff
bind__var_dir: /var/cache/bind
